#include <stdio.h>

/**
 * main - prints the phrase "Holeberton" on the screen
 * Return: 0 if exited properly, non-zero otherwise
*/

int main(void)
{
	char ch = "Holebertonn\n";
	putchar(ch);
	return (0);
}
