#include "main.h"

/**
 * function that tests and prints if an integer is positive or negative.
 * It should Return: 0
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
